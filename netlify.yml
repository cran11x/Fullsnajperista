# Netlify configuration for Fullsnajperista website

# For static site deployment
publish: website
build:
  command: echo "No build needed - static site"

# Redirect all routes to index.html (for SPA-like behavior)
[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200

